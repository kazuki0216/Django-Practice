[project]
name = "django-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.4",
    "djangorestframework>=3.16.1",
    "psycopg2-binary>=2.9.10",
    "pydantic-settings>=2.10.1",
    "ruff>=0.12.4",
]

[dependency-groups]
dev = [
    "poethepoet>=0.36.0",
]

[tool.poe.tasks]
dev = { shell = "uv run python manage.py runserver" }
